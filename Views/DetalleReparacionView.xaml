<UserControl x:Class="proyecto_paradigmas_2025.Views.DetalleReparacionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">

    <Grid Background="White" Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,20,0">
            <Button Content="← Volver" Command="{Binding VolverCommand}" HorizontalAlignment="Left" Margin="0,0,0,10"/>

            <TextBlock Text="{Binding ReparacionActual.Equipo.NombreCompleto}" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="{Binding ReparacionActual.Cliente.NombreCompleto}" FontSize="16" Foreground="Gray" Margin="0,0,0,20"/>

            <TextBlock Text="Estado Actual:" FontWeight="Bold"/>

            <ComboBox ItemsSource="{Binding EstadosDisponibles}" 
                      SelectedItem="{Binding EstadoSeleccionado}" 
                      IsEnabled="{Binding EsEstadoModificable}"
                      Margin="0,0,0,15"/>

            <TextBlock Text="Falla Reportada (Cliente):" FontWeight="Bold"/>
            <TextBox Text="{Binding ReparacionActual.Equipo.DescripcionFalla}" IsReadOnly="True" Background="#F0F0F0" Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="Diagnóstico Técnico y Solución:" FontWeight="Bold"/>
            <TextBox Text="{Binding ReparacionActual.DiagnosticoTecnico}" 
                     IsEnabled="{Binding EsEditable}"
                     Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,15"/>

            <Border Background="#E8F8F5" Padding="10" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Costos" FontWeight="Bold" Foreground="#16A085"/>
                    <DockPanel Margin="0,5">
                        <TextBlock Text="Mano de Obra:"/>
                        <TextBox Text="{Binding ReparacionActual.ManoDeObra}" 
                                 IsEnabled="{Binding EsEditable}"
                                 Width="100" HorizontalAlignment="Right"/>
                    </DockPanel>
                    <DockPanel Margin="0,5">
                        <TextBlock Text="Total Repuestos:" />
                        <TextBlock Text="{Binding ReparacionActual.TotalPagar}" FontWeight="Bold" HorizontalAlignment="Right"/>
                    </DockPanel>
                </StackPanel>
            </Border>
        </StackPanel>

        <Border Grid.Column="1" BorderBrush="#DDD" BorderThickness="1" Padding="15" CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Repuestos Utilizados" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

                <ListBox Grid.Row="1" ItemsSource="{Binding ReparacionActual.RepuestosUsados}" DisplayMemberPath="Nombre" Margin="0,0,0,10"/>

                <StackPanel Grid.Row="2">
                    <TextBlock Text="Agregar Repuesto del Inventario:"/>
                    <ComboBox ItemsSource="{Binding InventarioDisponible}" 
                              SelectedItem="{Binding ComponenteSeleccionado}"
                              DisplayMemberPath="Nombre"
                              Margin="0,5"/>

                    <Button Content="Agregar al Equipo (+ Costo)" 
                            Command="{Binding AgregarRepuestoCommand}"
                            IsEnabled="{Binding EsEditable}"
                            Background="#E67E22" Foreground="White" Margin="0,5,0,0"/>

                    <Button Content="GUARDAR CAMBIOS" 
                            Command="{Binding GuardarCambiosCommand}"
                            IsEnabled="{Binding EsEditable}"
                            Background="#27AE60" Foreground="White" Height="40" Margin="0,20,0,0" FontWeight="Bold"/>

                    <Button Content="📄 EMITIR FACTURA (TXT)" 
                            Command="{Binding FacturarCommand}"
                            Background="#8E44AD" Foreground="White" Height="40" Margin="0,10,0,0" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>