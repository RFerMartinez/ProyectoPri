<UserControl x:Class="proyecto_paradigmas_2025.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <DataTemplate x:Key="TarjetaKanban">
            <Border Background="White" Margin="0,0,0,10" CornerRadius="5" BorderBrush="#DDD" BorderThickness="1" Padding="10">
                <Border.Effect>
                    <DropShadowEffect Color="#EEE" BlurRadius="5" ShadowDepth="2" Direction="270"/>
                </Border.Effect>
                <StackPanel>
                    <DockPanel LastChildFill="True" Margin="0,0,0,5">
                        <TextBlock Text="📱" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Equipo.NombreCompleto}" FontWeight="Bold" Foreground="#2C3E50" TextTrimming="CharacterEllipsis"/>
                    </DockPanel>

                    <TextBlock Text="{Binding Id, StringFormat='ID #{0}'}" FontSize="10" Foreground="Gray" HorizontalAlignment="Right" Margin="0,-20,0,0"/>

                    <TextBlock Text="{Binding Cliente.NombreCompleto, StringFormat='Cliente: {0}'}" FontSize="11" Foreground="#555"/>
                    <TextBlock Text="{Binding FechaIngreso, StringFormat='Ingreso: {0:dd/MM/yyyy}'}" FontSize="11" Foreground="#555"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Background="#F4F6F7" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="20" Margin="0,0,10,0" BorderBrush="#27AE60" BorderThickness="0,4,0,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="💰 GANANCIA ESTIMADA" FontWeight="Bold" Foreground="#555" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding GananciaEstimada, StringFormat=c}" FontSize="36" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Center" Margin="0,5"/>
                    <TextBlock Text="(Ventas + Mano Obra - Costos)" FontSize="10" Foreground="Gray" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="20" Margin="10,0,0,0" BorderBrush="#3498DB" BorderThickness="0,4,0,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="⚙️ TOTAL EN PROCESO" FontWeight="Bold" Foreground="#555" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalEnProceso}" FontSize="36" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,5"/>
                    <TextBlock Text="Equipos Activos" FontSize="10" Foreground="Gray" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Margin="0,0,5,0" Background="#EAEDED" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Background="#F1C40F" CornerRadius="5,5,0,0" Padding="10">
                        <TextBlock Text="⏳ EN ESPERA" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                    </Border>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10">
                        <ItemsControl ItemsSource="{Binding ListaEnEspera}" ItemTemplate="{StaticResource TarjetaKanban}"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Column="1" Margin="5,0,5,0" Background="#EAEDED" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Background="#E67E22" CornerRadius="5,5,0,0" Padding="10">
                        <TextBlock Text="🔍 EN DIAGNÓSTICO" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                    </Border>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10">
                        <ItemsControl ItemsSource="{Binding ListaEnDiagnostico}" ItemTemplate="{StaticResource TarjetaKanban}"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Column="2" Margin="5,0,5,0" Background="#EAEDED" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Background="#3498DB" CornerRadius="5,5,0,0" Padding="10">
                        <TextBlock Text="🔧 EN REPARACIÓN" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                    </Border>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10">
                        <ItemsControl ItemsSource="{Binding ListaEnReparacion}" ItemTemplate="{StaticResource TarjetaKanban}"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Column="3" Margin="5,0,0,0" Background="#EAEDED" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Background="#27AE60" CornerRadius="5,5,0,0" Padding="10">
                        <TextBlock Text="✅ REPARADO" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                    </Border>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10">
                        <ItemsControl ItemsSource="{Binding ListaReparado}" ItemTemplate="{StaticResource TarjetaKanban}"/>
                    </ScrollViewer>
                </Grid>
            </Border>

        </Grid>
    </Grid>
</UserControl>