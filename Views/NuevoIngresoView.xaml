<UserControl x:Class="proyecto_paradigmas_2025.Views.NuevoIngresoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Background="White" Margin="20">
            <StackPanel MaxWidth="600" HorizontalAlignment="Left">

                <TextBlock Text="Registrar Nuevo Ingreso" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

                <Border BorderBrush="#DDD" BorderThickness="1" Padding="15" Margin="0,0,0,20" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="Datos del Cliente" FontWeight="Bold" Margin="0,0,0,10"/>

                        <TextBlock Text="Nombre Completo:"/>
                        <TextBox Text="{Binding NombreCliente}" Height="30" Margin="0,0,0,10"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="DNI:"/>
                                <TextBox Text="{Binding DNI}" Height="30"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Teléfono:"/>
                                <TextBox Text="{Binding Telefono}" Height="30"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Email:" Margin="0,10,0,0"/>
                        <TextBox Text="{Binding Email}" Height="30" Margin="0,0,0,10"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="Tipo de Equipo:" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,15">
                    <RadioButton Content="Celular" IsChecked="{Binding EsCelular}" GroupName="Tipo" Margin="0,0,20,0"/>
                    <RadioButton Content="Computadora" IsChecked="{Binding EsComputadora}" GroupName="Tipo"/>
                </StackPanel>

                <TextBlock Text="Marca:"/>
                <TextBox Text="{Binding Marca}" Height="30" Margin="0,0,0,10"/>

                <TextBlock Text="Modelo:"/>
                <TextBox Text="{Binding Modelo}" Height="30" Margin="0,0,0,10"/>

                <TextBlock Text="Descripción de la Falla:"/>
                <TextBox Text="{Binding Falla}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,10"/>

                <StackPanel Visibility="{Binding EsCelular, Converter={StaticResource BoolToVis}}">
                    <Border Background="#E8F8F5" Padding="10" CornerRadius="5" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Detalles de Celular" FontWeight="Bold" Foreground="#1ABC9C"/>
                            <TextBlock Text="IMEI:" Margin="0,5,0,0"/>
                            <TextBox Text="{Binding IMEI}" Height="30"/>

                            <TextBlock Text="Patrón / PIN:" Margin="0,5,0,0"/>
                            <TextBox Text="{Binding Patron}" Height="30"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Visibility="{Binding EsComputadora, Converter={StaticResource BoolToVis}}">
                    <Border Background="#EBF5FB" Padding="10" CornerRadius="5" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Detalles de Computadora" FontWeight="Bold" Foreground="#3498DB"/>
                            <TextBlock Text="Sistema Operativo:" Margin="0,5,0,0"/>
                            <TextBox Text="{Binding SistemaOperativo}" Height="30"/>

                            <CheckBox Content="Incluye Cargador" IsChecked="{Binding IncluyeCargador}" Margin="0,15,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Button Content="GUARDAR INGRESO" 
                        Command="{Binding GuardarCommand}"
                        Background="#27AE60" Foreground="White" FontWeight="Bold" Height="45" Margin="0,20,0,0"/>

            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>