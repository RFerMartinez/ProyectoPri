<UserControl x:Class="proyecto_paradigmas_2025.Views.ListaInventarioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900">

    <Grid Background="White" Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <TextBlock Text="Inventario de Repuestos" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock Text="Seleccione un ítem para editar o cree uno nuevo." Foreground="Gray"/>
            </StackPanel>

            <DataGrid Grid.Row="1" 
                      ItemsSource="{Binding Inventario}" 
                      SelectedItem="{Binding ComponenteSeleccionado}"
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      CanUserAddRows="False"
                      RowHeight="30" BorderBrush="#DDD">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                    <DataGridTextColumn Header="Repuesto" Binding="{Binding Nombre}" Width="*" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="60"/>
                    <DataGridTextColumn Header="Costo" Binding="{Binding PrecioCosto, StringFormat=c}" Width="80"/>
                    <DataGridTextColumn Header="Venta" Binding="{Binding PrecioVenta, StringFormat=c}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Border Grid.Column="1" Background="#F8F9F9" BorderBrush="#DDD" BorderThickness="1" CornerRadius="5" Padding="15">
            <StackPanel>
                <Button Content="+ NUEVO PRODUCTO" 
                        Command="{Binding NuevoCommand}"
                        Background="#2980B9" Foreground="White" Height="40" Margin="0,0,0,20" FontWeight="Bold"/>

                <TextBlock Text="Datos del Producto" FontWeight="Bold" Margin="0,0,0,10"/>
                <Separator Margin="0,0,0,15"/>

                <StackPanel IsEnabled="{Binding HaySeleccion}">

                    <TextBlock Text="Nombre del Repuesto:"/>
                    <TextBox Text="{Binding ComponenteSeleccionado.Nombre, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,10"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Stock:"/>
                            <TextBox Text="{Binding ComponenteSeleccionado.Stock}" Height="30"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Precios:" FontWeight="Bold" Margin="0,15,0,5"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Costo ($):"/>
                            <TextBox Text="{Binding ComponenteSeleccionado.PrecioCosto}" Height="30"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Venta ($):"/>
                            <TextBox Text="{Binding ComponenteSeleccionado.PrecioVenta}" Height="30"/>
                        </StackPanel>
                    </Grid>

                    <Button Content="GUARDAR" 
                            Command="{Binding GuardarCommand}"
                            Background="#27AE60" Foreground="White" Height="40" Margin="0,20,0,10" FontWeight="Bold"/>

                    <Button Content="Eliminar" 
                            Command="{Binding EliminarCommand}"
                            Background="#C0392B" Foreground="White" BorderThickness="0"/>
                </StackPanel>

            </StackPanel>
        </Border>

    </Grid>
</UserControl>